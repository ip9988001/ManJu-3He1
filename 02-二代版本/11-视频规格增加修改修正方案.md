以下是完整的修改指令，你可以直接存到 GitHub 仓库中，让 Agent 读取后逐份修改前四份文档：

```markdown
# 修改指令：前四份文档——将画幅比例从硬编码改为全局可配置

---

## 一、修改目标

仓库中第一份至第四份系统设计文档，所有涉及画幅比例的地方都写死为 9:16 竖屏或 1080x1920。需要将画幅比例改为全局可配置参数，与第五份文档中已确定的画幅可配置方案完全对齐。

第五份文档已经处理完毕，无需修改。本次只改第一份到第四份。

---

## 二、全局配置定义（所有修改都基于这个配置）

在项目的 config 系统中，画幅比例相关配置如下：

```python
# 项目级配置项
aspect_ratio = "9:16"  # 枚举值: "9:16" | "16:9" | "1:1"

# 以下为派生配置，由 aspect_ratio 自动决定
ASPECT_RATIO_MAP = {
    "9:16": {
        "output_resolution": (1080, 1920),       # 宽x高
        "resolution_label": "1080x1920",
        "image_gen_ar": "--ar 9:16",
        "subtitle_position": 0.80,                # 字幕在画面高度的80%位置
        "ken_burns_axis": "vertical",             # Ken Burns以纵向平移为主
    },
    "16:9": {
        "output_resolution": (1920, 1080),
        "resolution_label": "1920x1080",
        "image_gen_ar": "--ar 16:9",
        "subtitle_position": 0.88,
        "ken_burns_axis": "horizontal",           # Ken Burns以横向平移为主
    },
    "1:1": {
        "output_resolution": (1080, 1080),
        "resolution_label": "1080x1080",
        "image_gen_ar": "--ar 1:1",
        "subtitle_position": 0.82,
        "ken_burns_axis": "zoom",                 # Ken Burns以缓慢缩放为主
    },
}
```

所有后续修改中，凡是出现硬编码画幅值的地方，都应改为引用上述配置。

---

## 三、第一份文档的修改（全网竞品调研报告）

### 修改 3.1：各工具/平台的"适用性评估"维度

在第一份文档中，每个工具/平台的分析都包含一个"适用性评估"维度。请检查以下位置：

**搜索关键词**：`适用性评估`、`9:16`、`竖屏`、`1080x1920`

**修改方式**：如果适用性评估中提到"仅支持 9:16 竖屏"或"适合竖屏短剧"等描述，改为：

> 该平台支持多种画幅比例。在本系统中，通过项目配置的画幅比例（9:16 竖屏 / 16:9 横屏 / 1:1 方形）动态决定输出规格。

如果是反面描述（该平台不支持某种画幅），则保留原描述但补充：

> 注意：该平台不支持 16:9 横屏输出，在项目配置为 16:9 时需切换到其他替代方案。

### 修改 3.2：对比矩阵表格

**搜索关键词**：对比矩阵、综合对比、评估维度

**修改方式**：在对比矩阵表格中，如果有"支持画幅"或"输出规格"相关的行，将该行的值从固定的 9:16 改为"支持 9:16 / 16:9 / 1:1（具体取决于 API 参数）"。

### 修改 3.3：推荐方案组合

**搜索关键词**：`推荐的技术栈组合`、`推荐方案`

**修改方式**：在推荐的技术栈组合章节中，如果提到视频/图片输出规格，补充说明：

> 所有图像和视频生成 API 的输出画幅由项目配置统一控制，支持 9:16（1080x1920）、16:9（1920x1080）、1:1（1080x1080）三种模式。

---

## 四、第二份文档的修改（系统架构与数据流设计）

### 修改 4.1：XML 标签体系中的画幅相关描述

**搜索关键词**：`文生图提示词`标签的说明、`--ar`、`画幅`、`竖屏`

**修改位置**：模块 2.1（LLM 输出 XML 标签体系）

**修改方式**：在 `<文生图提示词>` 标签的说明中，如果有"必须加上画幅比例 --ar 9:16"的描述，改为：

> 文生图提示词末尾的画幅参数必须根据当前项目的画幅配置动态设置。竖屏模式使用 --ar 9:16，横屏模式使用 --ar 16:9，方形模式使用 --ar 1:1。严禁将画幅参数写死为某一固定值。

### 修改 4.2：Markdown 模板结构

**搜索关键词**：`9:16`、`1080x1920`、`竖屏`

**修改位置**：模块 2.2（本地 Markdown 模板结构）

**修改方式**：

1. 在 MD 模板的元数据区域中，如果有画幅相关字段，确保其描述为：

```
* **画幅比例：** 9:16（竖屏）/ 16:9（横屏）/ 1:1（方形），由项目配置决定
* **输出分辨率：** 根据画幅比例自动适配（1080x1920 / 1920x1080 / 1080x1080）
```

2. 在模板的填充示例中，如果文生图提示词包含 `--ar 9:16`，在示例末尾添加注释：

```
（注：此处的 --ar 9:16 为竖屏模式示例，实际值根据项目画幅配置动态替换）
```

### 修改 4.3：API 分发数据映射

**搜索关键词**：`文生图提示词 → 图像生成 API`、`尺寸`、`分辨率`

**修改位置**：模块 2.3（API 分发数据映射）

**修改方式**：在映射说明中，补充画幅参数的传递说明：

> 图像生成 API 调用时，除传递文生图提示词外，还需传递项目的画幅配置参数（如 --ar 9:16 / --ar 16:9 / --ar 1:1）。
>
> 视频生成 API 调用时，需传递对应的输出分辨率参数（1080x1920 / 1920x1080 / 1080x1080）。

### 修改 4.4：WebUI 界面描述中的画幅相关部分

**搜索关键词**：`9:16`、`竖屏`、`1080x1920`

**修改位置**：模块 6（WebUI 交互流程设计）中所有步骤的描述

**修改方式**：检查步骤一到步骤四中是否有写死的画幅描述，统一改为"根据项目配置的画幅比例"。特别注意：

- 步骤一的输入区如果有"短剧标准 9:16"等描述，改为"根据项目配置的画幅比例"
- 步骤四的预览区如果有"9:16 竖屏预览"等描述，改为"按项目配置的画幅比例预览"

### 修改 4.5：系统架构图

**搜索关键词**：架构图、数据流向图、拓扑图

**修改位置**：模块 9（系统架构图）

**修改方式**：如果架构图中包含输出规格节点，将固定的 1080x1920 改为 `config.output_resolution` 或标注"由画幅配置决定"。

### 修改 4.6：开发路线图

**搜索关键词**：各阶段交付物中的 `9:16`、`竖屏` 等描述

**修改位置**：模块 10（开发路线图）

**修改方式**：检查各阶段的交付物和验收标准中是否有硬编码画幅描述，统一改为可配置表述。

---

## 五、第三份文档的修改（LLM Meta-Prompt 设计）

### 修改 5.1：Prompt 3（分镜脚本拆解器）的 System Prompt

**搜索关键词**：`9:16`、`--ar 9:16`、`竖屏`、`画幅`

**修改位置**：Prompt 3 的完整 System Prompt 内容

**修改方式**：

1. 如果 System Prompt 中有"必须加上画幅比例 --ar 9:16"的指令，替换为：

> 文生图提示词末尾的画幅参数必须根据当前项目的画幅配置动态设置：
> - 竖屏模式（9:16）：使用 --ar 9:16
> - 横屏模式（16:9）：使用 --ar 16:9
> - 方形模式（1:1）：使用 --ar 1:1
> 严禁将画幅参数写死为某一固定值。

2. 如果 System Prompt 中有"短剧标准 9:16 竖屏"的描述，改为"根据项目配置的画幅比例"。

3. 在输出格式约定中，如果 `<文生图提示词>` 标签的示例包含固定的 `--ar 9:16`，在示例末尾加注释说明此参数需根据项目配置替换。

### 修改 5.2：Prompt 3 的文生图提示词质量标准与示例对比

**搜索关键词**：`--ar 9:16`、示例中的文生图提示词

**修改位置**：Prompt 3 的"文生图提示词质量标准与示例对比"章节

**修改方式**：

1. 在"好的提示词"示例中，如果末尾包含 `--ar 9:16`，在示例下方添加注释：

> （以上示例基于竖屏 9:16 模式。实际使用时，画幅参数应根据项目配置动态替换为 --ar 9:16、--ar 16:9 或 --ar 1:1）

2. 在示例对比表格中，如果"差的提示词"与画幅无关则无需修改。

### 修改 5.3：Prompt 4（文生图提示词精炼器）的五大核心要素

**搜索关键词**：`Quality Parameters`、`画质参数`、`--ar`、`画幅`

**修改位置**：Prompt 4 的五大核心要素中的第 5 项

**修改方式**：在 Quality Parameters（画质参数）的说明中，如果包含固定的画幅参数描述，改为：

> 画幅参数：根据项目配置动态设置（竖屏 --ar 9:16 / 横屏 --ar 16:9 / 方形 --ar 1:1），不得遗漏。

### 修改 5.4：Prompt 5（视频动态提示词精炼器）

**搜索关键词**：`9:16`、`竖屏`、画幅相关的描述

**修改位置**：Prompt 5 的 System Prompt 和优化规则

**修改方式**：检查是否有与画幅绑定的运动控制描述（如"竖屏模式下镜头以纵向运动为主"），如有，改为：

> 镜头运动方向应与项目配置的画幅比例匹配：
> - 竖屏（9:16）：以纵向运动为主（上下推拉摇移）
> - 横屏（16:9）：以横向运动为主（左右推拉摇移）
> - 方形（1:1）：以居中构图的推进/拉远为主

### 修改 5.5：全局约束规则

**搜索关键词**：`4.4 题材适配铁律`、全局约束中涉及画幅的描述

**修改位置**：四、全局约束规则

**修改方式**：在全局约束规则中新增一条（如果尚未存在）：

> **画幅参数铁律**：文生图提示词和视频动态提示词中涉及画幅比例、输出尺寸的参数，必须根据项目配置动态设置，严禁写死为单一值。三种画幅模式下的参数映射关系已在系统架构中定义，各 Prompt 必须严格遵守。

### 修改 5.6：完整示例演示

**搜索关键词**：`九、完整示例`章节中的所有提示词输出

**修改位置**：九、完整示例：从一段小说原文到完整分镜的全流程演示

**修改方式**：

1. 在示例开头声明：

> 以下示例基于画幅比例 9:16（竖屏模式）。如项目配置为 16:9 或 1:1，所有文生图提示词中的 --ar 参数和视频动态提示词中的镜头运动方向应相应调整。

2. 检查示例中所有出现 `--ar 9:16` 的位置，确认其上下文已包含画幅配置说明。

### 修改 5.7：Prompt 2（大纲提炼器）

**搜索关键词**：`9:16`、`竖屏`、`短剧节奏`

**修改位置**：Prompt 2 的 System Prompt

**修改方式**：Prompt 2 主要处理剧本大纲，通常不直接涉及画幅参数。但如果其中有"9:16 竖屏短剧节奏"等描述，改为"短剧/漫剧节奏"，去掉对特定画幅的绑定。

---

## 六、第四份文档的修改（API 调度、角色一致性与合成引擎）

### 修改 6.1：图像生成 API 适配器

**搜索关键词**：`width`、`height`、`1080`、`1920`、`9:16`、`--ar`、`aspect_ratio`

**修改位置**：模块 2.4（图像生成 API 适配器）

**修改方式**：

1. 在代码框架中，所有硬编码的尺寸参数改为从配置读取：

```python
# 修改前
width=1080, height=1920

# 修改后
width=config.output_resolution[0], height=config.output_resolution[1]
```

2. 画幅参数从配置读取：

```python
# 修改前
"--ar 9:16"

# 修改后
config.image_gen_ar  # 根据项目画幅配置自动映射
```

3. 如果适配器代码中有多处尺寸硬编码，全部统一替换。

### 修改 6.2：视频生成 API 适配器

**搜索关键词**：`resolution`、`1080x1920`、`1080`、`1920`、`9:16`

**修改位置**：模块 2.5（视频生成 API 适配器）

**修改方式**：

1. 输出分辨率从配置读取：

```python
# 修改前
resolution="1080x1920"

# 修改后
resolution=config.resolution_label  # "1080x1920" / "1920x1080" / "1080x1080"
```

2. 各 API 服务商的请求参数中，如果包含固定的 width/height，全部改为从配置读取。

### 修改 6.3：API 请求示例

**搜索关键词**：文档中展示的 API 请求/响应示例

**修改位置**：各适配器章节中的请求示例代码块

**修改方式**：在每个请求示例的代码块上方添加注释：

```python
# 注：以下请求示例基于竖屏 9:16 模式（1080x1920）。
# 实际使用时，width/height/aspect_ratio 参数应从项目配置中读取：
#   9:16 → width=1080, height=1920
#   16:9 → width=1920, height=1080
#   1:1  → width=1080, height=1080
```

### 修改 6.4：合成引擎 —— VideoSynthesizer 类

**搜索关键词**：`1080`、`1920`、`VideoSynthesizer`、`write_videofile`、`resolution`

**修改位置**：模块 5.5（VideoSynthesizer）

**修改方式**：

1. 输出分辨率从配置读取：

```python
# 修改前
final_clip.write_videofile(output_path, fps=24, codec="libx264", audio_codec="aac")

# 修改后
final_clip = final_clip.resize(config.output_resolution)
final_clip.write_videofile(output_path, fps=24, codec="libx264", audio_codec="aac")
```

2. 字幕定位从配置读取：

```python
# 修改前
txt_clip = txt_clip.set_position(('center', video_clip.h * 0.8))

# 修改后
txt_clip = txt_clip.set_position(('center', video_clip.h * config.subtitle_position))
```

### 修改 6.5：合成引擎 —— ImageAnimator 类

**搜索关键词**：`Ken Burns`、`ken_burns`、`ImageAnimator`

**修改位置**：模块 5.4（ImageAnimator）

**修改方式**：Ken Burns 效果的方向根据画幅自适应：

```python
# 修改前：写死为纵向平移
def ken_burns_effect(image_path, duration, motion_type="vertical_pan"):
    ...

# 修改后：根据画幅配置选择运动方向
def ken_burns_effect(image_path, duration, motion_type=None):
    if motion_type is None:
        axis_map = {
            "9:16": "vertical_pan",    # 竖屏：纵向缓慢平移
            "16:9": "horizontal_pan",  # 横屏：横向缓慢平移
            "1:1": "slow_zoom",        # 方形：缓慢缩放
        }
        motion_type = axis_map.get(config.aspect_ratio, "slow_zoom")
    ...
```

### 修改 6.6：合成引擎 —— EpisodeAssembler 类

**搜索关键词**：`EpisodeAssembler`、`1080`、`1920`、`resolution`、转场

**修改位置**：模块 5.6（EpisodeAssembler）

**修改方式**：拼接时的输出分辨率从配置读取，转场效果的尺寸与画幅对齐。

### 修改 6.7：FFmpeg 命令行方案

**搜索关键词**：`-s 1080x1920`、`scale=1080:1920`、`-vf`、`1080`、`1920`

**修改位置**：模块 5.8（FFmpeg 命令行方案）

**修改方式**：所有 FFmpeg 命令中的固定分辨率参数改为变量占位，并在命令上方添加说明：

```bash
# 注：以下命令中的 {WIDTH}x{HEIGHT} 为占位符，实际值根据项目画幅配置替换：
#   9:16 → 1080x1920
#   16:9 → 1920x1080
#   1:1  → 1080x1080

ffmpeg -i input.mp4 -vf "scale={WIDTH}:{HEIGHT}" -c:v libx264 output.mp4
```

### 修改 6.8：端到端数据流示例

**搜索关键词**：`六、端到端数据流完整示例`

**修改位置**：模块 6

**修改方式**：

1. 在示例开头声明画幅模式：

> 以下端到端示例基于 9:16 竖屏模式。如使用其他画幅，所有尺寸参数和画幅参数需相应替换。

2. 检查示例中所有 API 请求代码的尺寸参数，确保在实际代码中是从配置读取的（示例中可以展示具体值，但需有注释说明）。

3. 检查合成引擎调用代码中的分辨率参数，确保是从配置读取的。

### 修改 6.9：成本估算

**搜索关键词**：`成本估算`、`单集成本`、画幅相关的成本差异

**修改位置**：模块 8（成本估算）

**修改方式**：在成本估算说明中补充：

> 注：不同画幅比例可能影响 API 调用成本。例如，16:9 横屏（1920x1080）的像素总量与 9:16 竖屏（1080x1920）相同，但部分 API 服务商可能对不同分辨率有不同定价。1:1 方形的像素总量较少，成本可能略低。

### 修改 6.10：API 服务商选型推荐表

**搜索关键词**：`七、API 服务商选型推荐表`

**修改位置**：模块 7

**修改方式**：如果推荐表中有"支持画幅"相关的列，将值从固定的 9:16 改为"支持多种画幅（通过参数配置）"。对于不支持某些画幅的服务商，标注其限制。

---

## 七、修改验证检查清单

修改完成后，请逐一确认以下检查项：

### 第一份文档
- [ ] 全文搜索 `9:16`，确认无硬编码写死的情况
- [ ] 全文搜索 `1080x1920`，确认无硬编码写死的情况
- [ ] 全文搜索 `竖屏`，确认无绑定特定画幅的绝对化描述
- [ ] 对比矩阵中的画幅相关行已更新

### 第二份文档
- [ ] XML 标签体系中无固定画幅描述
- [ ] Markdown 模板元数据区域包含画幅配置字段
- [ ] MD 模板示例中画幅参数有动态替换说明
- [ ] API 数据映射表包含画幅参数传递说明
- [ ] WebUI 描述中无固定画幅描述
- [ ] 系统架构图中输出规格节点已更新

### 第三份文档
- [ ] Prompt 3 的 System Prompt 中画幅参数为动态引用
- [ ] Prompt 3 的示例对比中画幅参数有注释说明
- [ ] Prompt 4 的 Quality Parameters 包含动态画幅说明
- [ ] Prompt 5 的镜头运动方向与画幅联动
- [ ] 全局约束规则中包含画幅参数铁律
- [ ] 完整示例演示开头声明了画幅模式
- [ ] Prompt 2 中无绑定画幅的描述

### 第四份文档
- [ ] 所有图像生成适配器的尺寸参数从配置读取
- [ ] 所有视频生成适配器的分辨率参数从配置读取
- [ ] API 请求示例有画幅参数注释说明
- [ ] VideoSynthesizer 的输出分辨率从配置读取
- [ ] ImageAnimator 的 Ken Burns 方向与画幅联动
- [ ] EpisodeAssembler 的输出分辨率从配置读取
- [ ] FFmpeg 命令中的分辨率用变量占位
- [ ] 端到端示例有画幅模式声明
- [ ] 成本估算有画幅差异说明
- [ ] API 推荐表的画幅列已更新

### 跨文档一致性
- [ ] 第一份到第五份文档中，画幅配置的枚举值定义一致（9:16 / 16:9 / 1:1）
- [ ] 第一份到第五份文档中，三种画幅对应的分辨率定义一致
- [ ] 第一份到第五份文档中，字幕位置、Ken Burns 方向等派生参数定义一致
- [ ] 所有文档中 `config.aspect_ratio` 的引用方式一致

---

## 八、搜索替换快速操作指引

以下关键词组合可用于快速定位所有需要修改的位置。请对第一份到第四份文档逐一执行搜索：

| 搜索关键词 | 预期匹配数（估算） | 处理方式 |
|-----------|-------------------|---------|
| `9:16` | 10-20 处 | 逐一检查，改为动态引用或添加注释 |
| `1080x1920` | 5-15 处 | 替换为 `config.resolution_label` 或添加注释 |
| `1080, 1920` | 3-8 处 | 替换为 `config.output_resolution` |
| `--ar 9:16` | 5-10 处 | 替换为 `config.image_gen_ar` 或添加动态说明 |
| `竖屏` | 10-20 处 | 评估上下文，去除对特定画幅的绑定 |
| `h * 0.8` | 1-3 处 | 替换为 `h * config.subtitle_position` |
| `scale=1080` | 2-5 处 | 替换为变量 `scale={WIDTH}:{HEIGHT}` |
| `-s 1080x1920` | 1-3 处 | 替换为 `-s {WIDTH}x{HEIGHT}` |
| `width=1080` | 3-5 处 | 替换为 `width=config.output_resolution[0]` |
| `height=1920` | 3-5 处 | 替换为 `height=config.output_resolution[1]` |
| `ken_burns.*vertical` | 1-2 处 | 改为根据画幅动态选择 |

执行完所有搜索替换后，再通读一遍全文，确认语义通顺、逻辑连贯。

---

## 九、修改优先级

建议按以下顺序执行修改（效率最高）：

1. **第四份文档**（改动量最大，涉及大量代码）→ 第 6.1-6.7 节
2. **第三份文档**（涉及 Prompt 文本修改）→ 第 5.1-5.7 节
3. **第二份文档**（涉及架构描述修改）→ 第 4.1-4.6 节
4. **第一份文档**（改动量最小，主要是描述性文字）→ 第 3.1-3.3 节
5. **执行跨文档一致性检查** → 第七节的跨文档一致性检查项
```

---

这份指令覆盖了前四份文档中所有涉及画幅的位置，给出了精确的搜索关键词、修改方式和代码示例。你的 Agent 拿到后可以按文档逐份、逐节地执行修改，最后用验证清单确保没有遗漏。把它存到仓库根目录或 `docs/` 目录下即可。